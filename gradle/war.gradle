apply plugin: "war"

bootWar {
    mainClassName = "com.gamearea.poker.PokerApp"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]

}
